<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SariSariSales">

        <!-- Launcher activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.SariSariSales">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other activities -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.SariSariSales" />

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.SariSariSales" />

        <activity
            android:name=".WeeklyTransactionsActivity"
            android:exported="false"
            android:theme="@style/Theme.SariSariSales" />

        <activity
            android:name=".PosAllActivity"
            android:exported="false"
            android:theme="@style/Theme.SariSariSales" />

        <activity
            android:name=".SalesLoggingActivity"
            android:exported="false"
            android:theme="@style/Theme.SariSariSales" />

        <activity
            android:name=".SettingsAccountActivity"
            android:exported="false"
            android:theme="@style/Theme.SariSariSales" />

        <activity
            android:name=".SettingsNotificationsActivity"
            android:exported="false"
            android:theme="@style/Theme.SariSariSales" />

        <activity
            android:name=".SettingsAppearanceActivity"
            android:exported="false"
            android:theme="@style/Theme.SariSariSales" />

        <activity
            android:name=".SettingsHelpActivity"
            android:exported="false"
            android:theme="@style/Theme.SariSariSales" />

        <activity
            android:name=".TransactionsActivity"
            android:exported="false"
            android:theme="@style/Theme.SariSariSales" />



    </application>

</manifest>
